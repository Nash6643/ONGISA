[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "forge-refactor"
version = "0.1.0"
dependencies = [
    "pydantic>=2.0.0",
    "google-genai"
]

[tool.setuptools.packages.find]
where = ["src"]